<%= form_with model: group, local: true do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :avatar %>
    <%= f.file_field :avatar, onchange: 'previewFileWithId(preview)', class: 'form-control mb-3', accept: 'image/*' %>
    <%= f.hidden_field :avatar_cache %>
  </div>
  <br>
  <div class='mt-3 mb-3'>
    <%= image_tag @group.avatar.url, class: 'rounded-circle', id: 'preview', size: '100x100' %>
  </div>
  <%= f.submit (t 'defaults.register'), class: 'btn btn-primary' %>
<% end %>