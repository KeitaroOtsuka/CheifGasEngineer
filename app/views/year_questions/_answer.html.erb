<% question.choices.each do |question_choice| %>
  <div class="flex justify-center">
    <div class="mx-1 text-white bg-green-500 btn">
      <%= link_to results_path(choice_number: 1,question_id: question.id, choice_id: question_choice.id), id: "js-result-choice-for-question-#{question.id}", method: :post, local: true, class: "hover:text-white" do %>
        <%= question_choice.choice1 %>
      <% end %>
    </div>
    <div class="mx-1 text-white bg-green-500 btn">
      <%= link_to results_path(choice_number: 2,question_id: question.id, choice_id: question_choice.id), id: "js-result-choice-for-question-#{question.id}", method: :post, local: true, class: "hover:text-white" do%>
        <%= question_choice.choice2 %>
      <% end %>
    </div>
    <div class="mx-1 text-white bg-green-500 btn">
      <%= link_to results_path(choice_number: 3,question_id: question.id, choice_id: question_choice.id), id: "js-result-choice-for-question-#{question.id}", method: :post, local: true, class: "hover:text-white" do%>
        <%= question_choice.choice3 %>
      <% end %>
    </div>
    <div class="mx-1 text-white bg-green-500 btn">
      <%= link_to results_path(choice_number: 4,question_id: question.id, choice_id: question_choice.id), id: "js-result-choice-for-question-#{question.id}", method: :post, local: true, class: "hover:text-white" do%>
        <%= question_choice.choice4 %>
      <% end %>
    </div>
    <div class="mx-1 text-white bg-green-500 btn">
      <%= link_to results_path(choice_number: 5,question_id: question.id, choice_id: question_choice.id), id: "js-result-choice-for-question-#{question.id}", method: :post, local: true, class: "hover:text-white" do%>
        <%= question_choice.choice5 %>
      <% end %>
    </div>
  </div>
<% end %>