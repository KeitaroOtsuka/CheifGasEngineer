<% question.choices.each do |question_choice| %>
  <ul class="flex justify-center">
    <li class="mx-1">
      <%= link_to question_paper_path(choice_number: 1,question_paper_id: question_paper.id,question_id: question.id), method: :patch, remote: true, class: "text-white hover:text-white bg-green-500 btn" do %>
        <%= question_choice.choice1 %>
      <% end %>
    </li>
    <li class="mx-1">
      <%= link_to question_paper_path(choice_number: 2,question_paper_id: question_paper.id,question_id: question.id), method: :patch, remote: true, class: "text-white hover:text-white bg-green-500 btn" do%>
        <%= question_choice.choice2 %>
      <% end %>
    </li>
    <li class="mx-1">
      <%= link_to question_paper_path(choice_number: 3,question_paper_id: question_paper.id,question_id: question.id), method: :patch, remote: true, class: "text-white hover:text-white bg-green-500 btn" do%>
        <%= question_choice.choice3 %>
      <% end %>
    </li>
    <li class="mx-1">
      <%= link_to question_paper_path(choice_number: 4,question_paper_id: question_paper.id,question_id: question.id), method: :patch, remote: true, class: "text-white hover:text-white bg-green-500 btn" do%>
        <%= question_choice.choice4 %>
      <% end %>
    </li>
    <li class="mx-1">
      <%= link_to question_paper_path(choice_number: 5,question_paper_id: question_paper.id,question_id: question.id), method: :patch, remote: true, class: "text-white hover:text-white bg-green-500 btn" do%>
        <%= question_choice.choice5 %>
      <% end %>
    </li>
  </ul>
<% end %>